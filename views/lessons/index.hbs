Public Lessons


<h3>Welcome {{name}}</h3>
<p>Here are all the stories</p>
<h3>This is public lessons page.</h3>

{{#if lessons}}
<table>
    <thead>
        <tr>
            <th>Title</th>
            <th>Date</th>
            <th>Status</th>
        </tr>
    </thead>

<tbody>
    {{#each lessons}}
    <tr>
        <td>
            <a href="/lessons/{{_id}}">{{title}}</a>
        </td>
        <td>{{formatDate createdAt 'MMMM Do YYYY, h:mm:ss a'}}</td> 
        <td><span class="dash-status">{{status}}</span></td>


    </tr>

    {{/each}}
</tbody>
</table>
{{else}}
    <p>There are no current lessons.</p>

{{/if}}